import React from 'react';
import PropTypes from 'prop-types';

const WhoWeAre = props => {
    return (
        <div>
            
        </div>
    );
};

WhoWeAre.propTypes = {
    
};

export default WhoWeAre;