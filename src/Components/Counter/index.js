import React from 'react';
import PropTypes from 'prop-types';

const Counter = props => {
    return (
        <div>
            
        </div>
    );
};

Counter.propTypes = {
    
};

export default Counter;