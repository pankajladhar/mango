import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <Footer>
            <div className="container">
                
            </div>  
        </Footer>
    );
};

Footer.propTypes = {
    
};

export default Footer;