import React from 'react';
import PropTypes from 'prop-types';
import './Footer.scss';

const Footer = props => {
    return (
        <footer>
            <div className="container">
                div.addre
            </div>  
        </footer>
    );
};

Footer.propTypes = {
    
};

export default Footer;